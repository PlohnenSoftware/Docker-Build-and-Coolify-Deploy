__pycache__/
*.pyc

# The reference workflows this action was generalised from sit in the
# repository root and are not published. Anchored to the root, so .github/
# and the sub-action's manifest are unaffected.
/*.yml
/*.yaml
!/action.yml
